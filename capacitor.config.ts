import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prabhat.bubbleshooter',
  appName: 'Bubble Shooters Pro',
  webDir: 'dist',
};

export default config;
